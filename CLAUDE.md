# CLAUDE.md

@AGENTS.md

The instructions for this repository are agent-neutral and live in `AGENTS.md`. Read it before
changing anything — particularly the invariants section, where every item exists because breaking
it has already caused a bug on real hardware.

Keep guidance in `AGENTS.md` and `docs/`, not here, so every agent and editor reads the same
thing. This file exists only to point at it.
